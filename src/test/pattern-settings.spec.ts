import { PatternSettings } from '../lib/interface';

const example1: PatternSettings = {
  'lowercase': true,
  'regexp': /[a-zA-Z0-9\W_]+/g,
  'uppercase': true,
  'special': true,
  'numeric': true,
};
